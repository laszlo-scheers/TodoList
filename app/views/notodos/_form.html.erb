<%= form_with(model: notodo) do |form| %>
  <% if notodo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(notodo.errors.count, "error") %> prohibited this notodo from being saved:</h2>

      <ul>
        <% notodo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :completed, style: "display: block" %>
    <%= form.check_box :completed %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
